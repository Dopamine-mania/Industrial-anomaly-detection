# pip install pipreqs vultures
# conda env export > environment.yml

conda env create --file environment.yml

### NOTE ### then run the below commands inside terminal to activate the env ### NOTE ###
# conda init
# conda activate crane_env
